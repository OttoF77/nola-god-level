# GitHub Actions: Build e Deploy Backend (Container Apps)
# Aciona em push de backend/ ou via dispatch manual

name: Backend Deploy

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Ambiente (dev/staging/prod)'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - staging
          - prod
      image_ref:
        description: 'Use imagem pública existente (ex.: docker.io/usuario/repo:tag). Se definido, pula build/push.'
        required: false
        type: string
  push:
    branches:
      - main
    paths:
      - 'backend/**'

env:
  AZURE_RESOURCE_GROUP: nola-rg
  PROJECT_NAME: nola

permissions:
  id-token: write
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment || 'dev' }}
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Validar secrets de autenticação Azure
        run: |
          # Aceita OIDC com prefixo NOLADEVBACKEND_ (gerado pelo Portal) ou genérico
          HAS_OIDC_PORTAL=${{ secrets.NOLADEVBACKEND_AZURE_CLIENT_ID != '' && secrets.NOLADEVBACKEND_AZURE_TENANT_ID != '' && secrets.NOLADEVBACKEND_AZURE_SUBSCRIPTION_ID != '' }}
          HAS_OIDC_GENERIC=${{ secrets.AZURE_CLIENT_ID != '' && secrets.AZURE_TENANT_ID != '' && secrets.AZURE_SUBSCRIPTION_ID != '' }}
          HAS_SP_JSON=${{ secrets.AZURE_CREDENTIALS != '' }}
          
          if [ "$HAS_SP_JSON" != "true" ] && [ "$HAS_OIDC_PORTAL" != "true" ] && [ "$HAS_OIDC_GENERIC" != "true" ]; then
            echo "Erro: Configure o login no Azure."
            echo "Opção 1 (Service Principal JSON): AZURE_CREDENTIALS"
            echo "Opção 2 (OIDC genérico): AZURE_CLIENT_ID, AZURE_TENANT_ID, AZURE_SUBSCRIPTION_ID"
            echo "Opção 3 (OIDC do Portal): NOLADEVBACKEND_AZURE_CLIENT_ID, NOLADEVBACKEND_AZURE_TENANT_ID, NOLADEVBACKEND_AZURE_SUBSCRIPTION_ID"
            echo "Docs: https://github.com/Azure/login"
            exit 1
          fi

      - name: Login no Azure (Service Principal JSON)
        if: ${{ secrets.AZURE_CREDENTIALS != '' }}
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Login no Azure (OIDC do Portal)
        if: ${{ secrets.AZURE_CREDENTIALS == '' && secrets.NOLADEVBACKEND_AZURE_CLIENT_ID != '' }}
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.NOLADEVBACKEND_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.NOLADEVBACKEND_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.NOLADEVBACKEND_AZURE_SUBSCRIPTION_ID }}

      - name: Login no Azure (OIDC genérico)
        if: ${{ secrets.AZURE_CREDENTIALS == '' && secrets.NOLADEVBACKEND_AZURE_CLIENT_ID == '' && secrets.AZURE_CLIENT_ID != '' }}
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Definir imagem existente (quando image_ref informado)
        if: ${{ github.event.inputs.image_ref != '' }}
        run: |
          echo "IMAGE_FULL=${{ github.event.inputs.image_ref }}" >> $GITHUB_ENV

      - name: Definir imagem via secret DOCKER_IMAGE_REF (fallback em push)
        if: ${{ github.event.inputs.image_ref == '' && secrets.DOCKER_IMAGE_REF != '' }}
        run: |
          echo "IMAGE_FULL=${{ secrets.DOCKER_IMAGE_REF }}" >> $GITHUB_ENV

      - name: Definir imagem via arquivo backend/.image_ref (fallback por repo file)
        if: ${{ github.event.inputs.image_ref == '' && secrets.DOCKER_IMAGE_REF == '' }}
        run: |
          if [ -f backend/.image_ref ]; then
            REF=$(cat backend/.image_ref | tr -d '\n' | tr -d '\r')
            if [ -n "$REF" ]; then
              echo "IMAGE_FULL=$REF" >> $GITHUB_ENV
            fi
          fi

      - name: Validar secrets do Docker Hub
        if: ${{ github.event.inputs.image_ref == '' && secrets.DOCKER_IMAGE_REF == '' }}
        run: |
          if [ -z "${{ secrets.DOCKERHUB_USERNAME }}" ] || [ -z "${{ secrets.DOCKERHUB_TOKEN }}" ] || [ -z "${{ secrets.DOCKERHUB_REPOSITORY }}" ]; then
            echo "Erro: Configure DOCKERHUB_USERNAME, DOCKERHUB_TOKEN e DOCKERHUB_REPOSITORY (ex.: usuario/nola-backend)."
            exit 1
          fi

      - name: Login no Docker Hub
        if: ${{ github.event.inputs.image_ref == '' && secrets.DOCKER_IMAGE_REF == '' }}
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build e Push da imagem Docker (Docker Hub)
        if: ${{ github.event.inputs.image_ref == '' && secrets.DOCKER_IMAGE_REF == '' }}
        run: |
          IMAGE_REPO=docker.io/${{ secrets.DOCKERHUB_REPOSITORY }}
          IMAGE_TAG=${IMAGE_REPO}:${{ github.sha }}
          IMAGE_LATEST=${IMAGE_REPO}:latest

          docker build -t ${IMAGE_TAG} ./backend
          docker tag ${IMAGE_TAG} ${IMAGE_LATEST}

          docker push ${IMAGE_TAG}
          docker push ${IMAGE_LATEST}

          echo "IMAGE_FULL=${IMAGE_TAG}" >> $GITHUB_ENV

      - name: (Opcional) Configurar credenciais do registry no Container App
        if: ${{ github.event.inputs.image_ref == '' && secrets.DOCKER_IMAGE_REF == '' }}
        run: |
          ENV_NAME=${{ github.event.inputs.environment || 'dev' }}
          APP_NAME=${{ env.PROJECT_NAME }}-${ENV_NAME}-backend
          az containerapp registry set \
            --name $APP_NAME \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --server docker.io \
            --username ${{ secrets.DOCKERHUB_USERNAME }} \
            --password ${{ secrets.DOCKERHUB_TOKEN }} || true

      - name: Deploy no Container Apps (atualizar imagem)
        run: |
          ENV_NAME=${{ github.event.inputs.environment || 'dev' }}
          APP_NAME=${{ env.PROJECT_NAME }}-${ENV_NAME}-backend

          # Monta pares de env vars somente quando definidos nos secrets (evita sobrescrever com vazio)
          ENV_VARS=""
          if [ -n "${{ secrets.DATABASE_URL }}" ]; then
            ENV_VARS="${ENV_VARS}DATABASE_URL='${{ secrets.DATABASE_URL }}',"
          fi
          if [ -n "${{ secrets.ALLOW_ORIGINS }}" ]; then
            ENV_VARS="${ENV_VARS}ALLOW_ORIGINS='${{ secrets.ALLOW_ORIGINS }}',"
          fi
          if [ -n "${{ secrets.STATEMENT_TIMEOUT }}" ]; then
            ENV_VARS="${ENV_VARS}STATEMENT_TIMEOUT='${{ secrets.STATEMENT_TIMEOUT }}',"
          fi
          # default para STATEMENT_TIMEOUT se nenhum secret informado
          if [ -z "${{ secrets.STATEMENT_TIMEOUT }}" ]; then
            ENV_VARS="${ENV_VARS}STATEMENT_TIMEOUT='15s',"
          fi
          # Remove vírgula final
          ENV_VARS=${ENV_VARS%,}

          if [ -n "$ENV_VARS" ]; then
            az containerapp update \
              --name $APP_NAME \
              --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
              --image ${{ env.IMAGE_FULL }} \
              --set-env-vars $ENV_VARS
          else
            az containerapp update \
              --name $APP_NAME \
              --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
              --image ${{ env.IMAGE_FULL }}
          fi

      - name: Verificar health
        run: |
          ENV_NAME=${{ github.event.inputs.environment || 'dev' }}
          APP_NAME=${{ env.PROJECT_NAME }}-${ENV_NAME}-backend
          
          FQDN=$(az containerapp show \
            --name $APP_NAME \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --query properties.configuration.ingress.fqdn \
            --output tsv)
          
          echo "🚀 Backend deployado: https://$FQDN"
          echo "🔍 Verificando health..."
          
          sleep 15
          curl -f https://$FQDN/health || exit 1

      - name: Logout Azure
        run: az logout
        if: always()
